# MaxMind test fixtures

Both `.mmdb` files are vendored from <https://github.com/maxmind/MaxMind-DB/tree/main/test-data>
under that project's Apache-2.0 license. They are synthetic test data —
not real geo records — and exist only to exercise the maxminddb code
path in unit tests. Update by re-downloading from the same URLs.
